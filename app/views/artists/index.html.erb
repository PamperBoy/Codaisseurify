<% @artists.each do |artist| %>
  <p>
    <%= link_to artist.name, artist, id: "artist-#{artist.id}"%>
  </p>





<%end%>
<%= link_to "Add artist", new_artist_path(@artist), class: "btn btn-primary" %>
